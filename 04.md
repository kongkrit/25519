```
write a single html JS CSS with specification and 3 sections. download output file.

Title and h1: “Web Crypto ECDH25519 Demo”

General Specifications:

- never show the "general specifications" in the generated code.
- assume Ed25519 is available on the target browser.
- separate DOM handling from internal logic. Minimal DOM wire-up to call internal logic. Make internal logic pure JS without messing with DOM.
- every textarea has button#copy to the bottom to copy its content to clipboard.
- always line break from one sub-section to the next.
- layout is strictly single column.
- each subsection is independent, and only use parameters specified in that subsection to generate output.
- for each subsection, if there’s insufficiency or unused parameters, give warning and explanation.
- give only terse, short text for explanation, headings, etc.

CSS Specifications:

- never show "CSS specifications" in the generated CSS.
- globally parameterize colors and element sizes using vars in :root { } section
- use rem units for font-size, margin, padding, border-width, border-radius, gap
- use body { font-family: monospace, sans-serif; text-align: center; margin: 0; }
- use the following colors:
  - body { background-color: #202020; color: #e0e0e0; padding: 1rem; }
  - textarea { background-color: #2e2e2e; color: #dedede; border-color: #484848; 
               width: 100%; box-sizing: border-box; overflow-y: auto; resize: none; padding: 1rem; border-radius: 0.4rem; }
  - button { background-color: #3a3a3a; color: #dedede; font-family: monospace, sans-serif font-size: 1.4rem; 
             border-radius: 0.4rem; padding: 0.8rem 1.6rem; cursor: pointer; transition: background-color 0.2s ease; }
  - button:hover { background-color: #555555; }
  - label { display: block; text-align: left; padding-left: 0.4rem; }
  - h2 { font-weight: normal; margin-top: 0.6rem; margin-bottom: 0.6rem; }
- do not generate extra CSS properties than specified above.

Section 1 — generate keys:

- button#generateEd25519 generates a private key for Ed25519
- show private key of Ed25519 in textarea#privatekeyEd25519. #privatekeyEd25519 should be only 2 rows.
- shoe public key of Ed25519 in textarea#publickeyEd25519. #publickeyEd25519 should be only 2 rows. Ensure no private info leakage.

Section 2 — sign message:

- textarea#message editable with default text “Hello message signature”. should be 4 rows.
- button#sign #message by #privatekeyEd25519 to generate textarea#signature

Section 3 — verify:

- button#verify uses #message, #publickeyEd25519, and #signature. give out original message and signature in textarea#verified. textarea#verified should be 7 rows. Give validity status of #signature against #message in textlabel#valid-message-signature
```
